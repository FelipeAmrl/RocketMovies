import styled from 'styled-components';

export const Container = styled.div`
    width: 100%;
    padding-right: 1.2rem;

    p {
        font-size: 1.6rem;
        font-weight: 400;
        line-height: 2.1rem;
        text-align: justify;

        flex: 1;
    }
`